# Best practices — carte IA (degré 1) [STUB]

> Conventions de code, nommage, sécurité, tests.

> Statut : stub. À remixer dans `docV2`.

## Repères

- Nommage : kebab-case fichiers, PascalCase classes, camelCase méthodes, snake_case variables/tables.
- Backend : `declare(strict_types=1)`, Form Requests pour la validation, Pint + PHPStan.
- Frontend : Composition API, DocBlocks JSDoc, pas de classes Tailwind dynamiques.
- Doc : « pas d'historique », un concept = un fichier (`docs/DOCUMENTATION_GUIDE.md`).

## Descendre

- Doc existante (L2) : `docs/10-BestPractices/` (`NAMING_CONVENTIONS.md`, `SECURITY_PRACTICES.md`, `TESTING_PRACTICES.md`, `PROJECT_STRUCTURE.md`).
