# Dev guides — carte IA (degré 1) [STUB]

> Outils de développement : CLI projet `project:*`, performance, génération d'images.

> Statut : stub. À remixer dans `docV2`.

## Repères

- CLI projet : commandes `project:*` (init, migrate seed, backup, deps, review, cron). Voir `docs/40-DevGuides/PROJECT_CLI.md`.
- Dev local : `composer run dev` lance serveur + queue + Vite (voir `composer.json`).

## Descendre

- Doc existante (L2) : `docs/40-DevGuides/`.
- Ops/admin : voir [features/_ai.md](../features/_ai.md) (Admin / Ops).
