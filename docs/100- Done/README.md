# 100-Done

Ce dossier est réservé aux **résumés descriptifs** de fonctionnalités livrées ou de décisions d’architecture, lorsqu’ils servent de référence (et non de journal chronologique).

Pour la documentation technique et les bonnes pratiques, voir :

- **Projet** : `docs/00-Project/`
- **Bonnes pratiques** : `docs/10-BestPractices/`
- **Contenu & entités** : `docs/20-Content/`
- **UI & Atomic Design** : `docs/30-UI/`
- **Fonctionnalités** : `docs/50-Fonctionnalités/`

### Livraisons

- **Changelog 1.3.2 (2026-05)** : journal public joueur/MJ dans `storage/app/public/changelog/1.3.2.md` ; semver via `PUBLIC_CHANGELOG_SEMVER`.
- **Transverse release 1.3.2 (2026-05)** : feedback email récap connectés ; contrôles audit/PHPStan/ESLint + tests Feature ciblés. Doc : [RELEASE_1.3.2_VERIFICATION.md](../10-BestPractices/RELEASE_1.3.2_VERIFICATION.md).
- **Hors gel 1.3.2 — outillage & socle (2026-05)** : `project:init:verify` (+ `--verify` sur init) ; commandes `characteristics:definitions-progress`, `definitions-apply`, audit `--report` ; 282 JSON à 100 % (gate qualité) ; toggle global notifications dans Paramètres ; recette [MANUAL_RECIPE_RELEASE_1.3.2.md](../10-BestPractices/MANUAL_RECIPE_RELEASE_1.3.2.md) ; Playwright smoke + `pnpm run test:a11y` ; script `pnpm run audit:docs`. Index calibration : [CALIBRATION_282_INDEX.md](../50-Fonctionnalités/Characteristics-DB/CALIBRATION_282_INDEX.md).
- **Phase K polish UI/UX (2026-05)** : Alert glass contrasté ; toasts 14 s + pause focus/survol ; skip link + `#main-content` ; responsive Main/Header. Doc : [PROJECT_UX_PHASE_K.md](../10-BestPractices/PROJECT_UX_PHASE_K.md).
- **Phase J commandes projet (2026-05)** : `project:seed` (données locales sans DofusDB) ; `project:refresh --fast` ; `project:cron --update` ; dossier `Commands/Scrapping/Effects/`. Doc : [PROJECT_COMMANDS_PHASE_J.md](../10-BestPractices/PROJECT_COMMANDS_PHASE_J.md).
- **Phase I recherche globale (2026-05)** : moteur header (`SearchInput`) avec overlay flou, filtres entités/états, résultats groupés par type ; API `GET /api/global-search` + `meta.hasMore` / chargement progressif. Doc : [ENTITY_GLOBAL_SEARCH_PHASE_I.md](../10-BestPractices/ENTITY_GLOBAL_SEARCH_PHASE_I.md).
- **Phase H admin & compte (2026-05)** : menus Gestion du contenu / Espace admin ; tableaux de bord (entités × statuts, récap utilisateurs) ; zone sensible ; mon compte (légal). Doc : [ADMIN_ACCOUNT_PHASE_H.md](../10-BestPractices/ADMIN_ACCOUNT_PHASE_H.md).
- **Phase G spécialisations & classes (2026-05)** : sous-pages menu bibliothèque par classe/spécialisation ; fiche CMS liée ; variantes sorts max 4 ; blocs relations par niveau ; champs legacy classe masqués. Doc : [ENTITY_SPECIALIZATIONS_PHASE_G.md](../10-BestPractices/ENTITY_SPECIALIZATIONS_PHASE_G.md).
- **Phase F bugs entités (2026-05)** : Ziggy `capability` ; état sort en vue minimal ; kref sorts dans effet item (full) ; monstre (langues visibles, dock édition) ; routes bibliothèque états/traits ; panoplie minimal (bonus + équipements texte au survol). Doc : [ENTITY_BUGS_PHASE_F.md](../10-BestPractices/ENTITY_BUGS_PHASE_F.md). Tests : `entityRouteRegistry.test.js`.
- **Phase E caractéristiques & normes MJ (2026-05)** : bouton d’aide **normes / graphique** sur les champs d’édition liés aux caractéristiques (`CharacteristicNormsHelpButton`, monstre via `byMonsterField`, sorts via `characteristics-group="spell"`) ; fallback colonne **Effets** sans chips (`effect_usages_summary` puis HTML `effect`) ; commande `php artisan characteristics:audit-definitions`. Doc : [ENTITY_CHARACTERISTICS_PHASE_E.md](../10-BestPractices/ENTITY_CHARACTERISTICS_PHASE_E.md). Tests : `spell-effect-summary-fallback.test.js`, `CharacteristicDefinitionReaderTest`.
- **Phase D TipTap & mentions @ (2026-05)** : recherche `@` ordonnée **caractéristiques → sections → pages → entités**, plafond **12** résultats + scroll ; aperçu section **~10 blocs** (troncature API fin de paragraphe) ; popover section scroll vertical sans débordement horizontal ; styles unifiés `kref-rich-preview-panel` (tooltips carac./entité + popover section). Doc : [RICH_TEXT_KREF_PREVIEW.md](../50-Fonctionnalités/CMS/RICH_TEXT_KREF_PREVIEW.md). Tests : `useRichReferenceSearch.test.js`, `CmsSectionPreviewApiTest`.
- **Phase C vues entité — minimal / line / texte / full / edit (2026-05)** : modèle officiel **full** (fiche page ou modal via `*ViewFull.vue`, 16 entités), **minimal**, **line**, **texte**, **edit** ; infra `resolveEntityViewComponent`, `useEntityViewFormat`, `EntityViewFullWrapper`, `EntityModal` ; tableaux sans bandeau densité ; raccourcis Q8 (sélection, double-clic modal full, Ctrl/Cmd → page Show, Alt → édition ou notification) ; création Q9 (champs minimaux puis édition complète, `RedirectsAfterEntityCreate`, `entity-create-config`). Doc : [ENTITY_VIEWS.md](../30-UI/ENTITY_VIEWS.md), [ENTITY_VIEWS_PHASE_C.md](../10-BestPractices/ENTITY_VIEWS_PHASE_C.md).
- **Phase B légal / changelog / RGPD (2026-05)** : Markdown public versionné depuis `storage/app/public/legal/` et `changelog/` ; flux HTTP **sans URL absolue de dev** via `legal.{cgu,politique-donnees,cookies}` et `changelog.feed/{semver}` ; variable **`PUBLIC_CHANGELOG_SEMVER`** (`config/releases.php`) ; page CMS **Cookies** (synthèse) seedée sous « Légales » ; bannière consentement reliant les pages critiques via Ziggy (**`route('pages.show')`**). Doc : [**LEGAL_CHANGELOG_PHASE_B.md**](../10-BestPractices/LEGAL_CHANGELOG_PHASE_B.md). Tests : `tests/Feature/Web/LegalAndChangelogMarkdownRoutesTest.php`.
- **Phase A droits / visibilité (2026-05)** : matrice admin « Gérer l’affichage » (`application_settings`, `EntityDisplayVisibilityService`), intégration dans `BaseEntityPolicy` / `BreedPolicy`, invalidation SSR des props permissions via **`EntityPermissionService::bumpPermissionsCacheRevision()`** après sauvegarde. Défauts création page/section : `read_level` invité et `write_level` MJ (décision Q6). État workflow (`raw`, `draft`, `playable`, `archived`) : pastille + menu dans la barre d’actions. Doc : [`ENTITY_VISIBILITY_PHASE_A.md`](../10-BestPractices/ENTITY_VISIBILITY_PHASE_A.md).
- **Spécialisations (Specialization) — refonte complète (2026-05)** : CRUD web finalisé (`index/show/create/store/edit/update/delete/pdf`) avec page `Show` dédiée, édition enrichie et synchronisation relationnelle par endpoints dédiés : `spells`, `capabilities`, `creature_traits`, `consumables`, `resources`, `items`, `sections`. Ajout des pivots niveau/quantité (`specialization_spell`, `consumable_specialization`, `resource_specialization`, `item_specialization`, `section_specialization`) et extension du schéma (`short_description`). Côté UI : table/filters alignés (nom, descriptions, capacités, sorts), vue `line` ajoutée, actions index/modal complétées (PDF/suppression), adapter frontend dédié `specialization-adapter`.
- **Refonte service images (2026-05)** : Spatie comme socle + `EntityImageMediaService` pour les uploads ; alignement `HasMediaCustomNaming` (`[slug]`, `[key]`, …), conversions `thumb` sur icônes Breed/Caractéristique ; `Image.vue` avec skeleton non bloquant ; `ImageService.getThumbnailUrl()` vers `/media/thumbnails/...` ; `ImageService` PHP (Intervention v3 `contain`/`cover`, MIME corrects, répertoires cache, cache sans tags si store non taggable) ; migration des `<img>` tableaux / Line / Minimal vers l’atome `Image` ; tests `tests/Feature/Media/MediaThumbnailRouteTest.php`, `tests/unit/utils/ImageService.test.js`. Doc : [IMAGE-GENERATE.md](../40-DevGuides/IMAGE-GENERATE.md), [SPATIE_MEDIA_LIBRARY.md](../50-Fonctionnalités/Medias/SPATIE_MEDIA_LIBRARY.md).
- **Dock d’actions d’édition — `EditActionDock` (2026-05)** : molécule `resources/js/Pages/Molecules/action/EditActionDock.vue` (Enregistrer + Annuler / Reset / actions custom) ; desktop : dock bas-droite, secondaires révélées au survol ou au focus ; mobile : boutons en flux en bas de contenu. Intégration dans `EntityEditForm` (remplace le double FAB + barre quand formulaire long / capacité), admin caractéristiques (Enregistrer + Supprimer), bloc prix item (`ItemPriceEditSection`), éditeurs satellites classe (`BreedElementOrientationsEditor`, `BreedSpellSlotsEditor`, `BreedCapabilitiesEditor`), `EntityRelationsManager`, groupe d’effets (`EffectGroupEditorForm`, dont fiche sort via `SpellEffectsUnifiedSection`). Dock non fixe dans les sections pour éviter plusieurs FAB sur une même page. Index `molecules.index.json`, tests `tests/unit/molecules/EditActionDock.test.js`.
- **Classes (Breed) — emplacements de sorts (2026-05)** : la table pivot `breed_spell` porte `character_level`, `slot_index`, `choice_order` (clé `id`, contrainte d’unicité par couple breed/niveau/emplacement/sort). Synchronisation `PATCH …/spells` via `UpdateBreedSpellsRequest` (payload `{ spellId: { character_level, slot_index, choice_order } }`). `BreedResource` expose `spell_slots` groupé ; fiche `BreedViewFull` affiche par niveau et emplacement ; PDF (`sorts_par_emplacements`). Scrapping : `RelationImportStack` attache avec pivots (slots séquentiels au niveau 1 ou conservation des pivots existants). `EntityRelationsManager` : pivots breed, libellés FR, détection des changements incluant les pivots.
- **Classes (Breed) — liste, fiches et édition (2026-05)** : CRUD web complet (`BreedController`), fiche publique `Show`, édition `Edit` avec `EntityEditForm` + synchronisation des sorts (`PATCH …/spells`, `EntityRelationsManager`). Liste alignée sur les autres entités (bulk corrigé, PDF, suppression confirmée, `watch` permissions, création via modal `entity-type="breed"`). `EntityModal` passe la prop `breed` aux vues. Policy : `createAny`, `deleteAny`, `manageAny` pour les capabilities du tableau API. Route publique `show` avec contrainte `whereNumber('breed')`. Tests : `tests/Feature/Entity/BreedControllerTest.php`.
- **Formules créature stables + objets (2026-04)** : exposition systématique des bonus d’équipement en variables `[clé_object]` dans `CreatureObjectBonusToCreatureVariables`, normalisation des formules seed `_creature` (maîtrises `*_mastery_creature`, bonus objets `*_object`, sauvegardes avec `save_*_object`), validateur de placeholders `CreatureFormulaPlaceholderValidator` et commande `characteristics:validate-creature-formula-placeholders`. Documentation : [CREATURE_FORMULES_STABLES.md](../400- Jeu/410- Ressources/CREATURE_FORMULES_STABLES.md).
- **Références TipTap `@` — finalisation insertion/hover (2026-04)** : ajout d’un parseur `@` robuste avec préfixes typés (`@carac:`, `@section:`, `@<typeEntite>:`), recherche contextualisée limitée à 12 résultats avec annulation des requêtes en vol, et menu toolbar pour insérer les déclencheurs. Unification des types d’entités kref via registres dédiés front (`krefEntityRegistry.js`) et back (`KrefEntityRegistry.php`) pour aligner recherche, preview et validation. Ajout d’un cache mémoire pour les aperçus de sections au survol (`krefSectionPreviewCache`) afin d’éviter les rechargements répétés.
- **Surcharges visuelles par valeur des caractéristiques (2026-04)** : colonne JSON `value_overrides` sur `characteristics` permettant de définir icône, couleur et sous-texte conditionnels selon la valeur. Résolution frontend via `resolveValueOverride` (priorité sur `icon_false`). Affichage du sous-texte dans les tooltips (`CharacteristicPropertyTooltip`, `CharacteristicBoolean`, `SpellUsageCharacteristicTooltipPanel`). Données pré-configurées pour PO, ligne de vue, magie/physique, rituel, portée modifiable, réaction. Administration via section dédiée dans la page caractéristiques. Voir [SURCHARGES_VISUELLES_PAR_VALEUR.md](../50-Fonctionnalités/Characteristics-DB/SURCHARGES_VISUELLES_PAR_VALEUR.md).
- **Refonte UI propriétés / caractéristiques (2026-04)** : pipeline unique avec `useCharacteristicViewModel`, composants `CharacteristicProperty` + `CharacteristicPropertyTooltip`, wrappers `PropertyDisplay`, `CharacteristicFormula`, `CharacteristicChip`, `EntityPropertyDisplay` ; groupes/cartes (`CharacteristicGroup`, `CharacteristicsCard`) enrichis par `runtime` ; chargement `resolved-stats` côté vues créature via `useCreatureResolvedStats`. Doc : [PROPERTY_DISPLAY_SYSTEM.md](../30-UI/PROPERTY_DISPLAY_SYSTEM.md).
- **Lanceur de dés (outil modal) (2026-03-19)** : Outil de lancer de dés accessible via le dropdown « Outils » du footer de l'Aside. Formule ndX (ex: 2d6+3, 3d10-1, 4d6/2) avec opérations +, -, *, / et notations alternatives (x, :). Affichage min/max/moyenne en temps réel, raccourcis d4, d6, d8, d10, d12, d20, d100. Lancer via bouton ou touche Entrée. Voir `resources/js/Utils/dice/diceParser.js`, `DiceRollerModal.vue`, `ToolsFooterDropdown.vue`.
- **Confirmation mot de passe — délai d'inactivité** : Middleware `RequirePasswordWithInactivity` remplaçant le comportement par défaut de Laravel. Une confirmation débloque l'accès ; la sécurité se réactive après 1 h d'inactivité (config `AUTH_PASSWORD_INACTIVITY_TIMEOUT`). Prop Inertia `auth.password_recently_confirmed` pour éviter de redemander le mot de passe à chaque chargement de page (ex. Scrapping).
- **Vérification confidentialité GitHub** : [VERIFICATION_CONFIDENTIALITE_GITHUB.md](./VERIFICATION_CONFIDENTIALITE_GITHUB.md) — audit des données personnelles (emails, clés, fichiers sensibles), corrections appliquées (scripts Playwright + neutralisation des références à l’ancien site dans la doc règles), 2 mentions conservées pour l’ancienne adresse du JDR.
- **Scrapping (backend)** : [FINALISATION_SCRAPPING_BACKEND.md](./FINALISATION_SCRAPPING_BACKEND.md) — limites depuis config, initiative monster, import range via runMany, robustesse, tests, doc.
- **Scrapping (config)** : [INVENTAIRE_JSON_ET_MIGRATION_BDD_UI.md](../50-Fonctionnalités/Scrapping/INVENTAIRE_JSON_ET_MIGRATION_BDD_UI.md) — inventaire des JSON (rôle, clés utilisées), évaluation transfert BDD + UI admin, plan de migration en 3 phases.
- **Scrapping (mapping BDD + UI)** : migration `scrapping_entity_mappings` / `scrapping_entity_mapping_targets`, modèles, `ScrappingMappingService`, intégration dans `ConfigLoader` (mapping BDD prioritaire, fallback sur le mapping des JSON d'entité si BDD vide), contrôleur admin, page Vue « Mapping scrapping » (liste par entité, CRUD règles + cibles). Lien dans le menu Admin.
- **Ressources — relations unidirectionnelles (2026-03-19)** : Ne charger que les relations « ressources qui composent » (`recipeIngredients`), sans les relations inverses (items, consommables, créatures utilisant cette ressource). Modifications : (1) **DofusDB** : `$populate=false` sur fetchOne items pour éviter `recipesThatUse` ; (2) **ResourceController** : retrait de consumables, items, creatures du `load` (show/edit) et de l’index ; (3) **ResourceTableController** : retrait de `withCount` items/consumables/creatures ; (4) **Edit/Show** : suppression des sections Objets, Consommables, Créatures (relations inverses) ; (5) **ResourceResource** : exposition conditionnelle (`whenLoaded`) pour items/consumables/creatures conservée mais données non chargées par défaut. Gain : requêtes plus légères et rapides.
- **Ressources — relations unidirectionnelles (2026-03-19)** : On ne charge plus les relations inverses (objets, consommables, créatures qui *utilisent* cette ressource). Conservé : `recipeIngredients` (ressources qui *composent* cette ressource). Modifs : (1) **DofusDB** : `$populate=false` dans item fetchOne pour éviter `recipesThatUse`. (2) **ResourceController** : suppression de `consumables`, `creatures`, `items` du `load` (show/edit/index) et des props `availableItems`, `availableConsumables`, `availableCreatures` en create/edit. (3) **ResourceTableController** : `withCount` limité à `recipeIngredients` (plus items/consumables/creatures). (4) **Vues** : sections « Objets utilisant cette ressource », « Consommables », « Créatures » retirées de Show.vue et Edit.vue. (5) **ResourceResource** : relations `consumables`, `creatures`, `items` retirées du whenLoaded. Les routes `updateItems`, `updateConsumables`, `updateCreatures` restent disponibles si besoin.
- **Scrapping — types équipements et consommables (2026-03-19)** : Ajout des formatters `resolveItemTypeId` et `resolveConsumableTypeId` pour remplir `item_type_id` (équipements) et `consumable_type_id` (consommables) lors du scrapping. Le mapping `item.json` inclut désormais ces deux champs. Même logique que `resolveResourceTypeId` : vérification du superType DofusDB (equipment pour items, consumable pour consumables), firstOrCreate si catalog injecté, sinon lookup existant uniquement. Doc : PROPRIETES_ITEMS_RESOURCES_CONSUMABLES.md (sections 5b, 5c).
- **Scrapping — types de ressources (2026-03-19)** : Correction de la pollution de `resource_types` par des types consommables et équipements. Le formatter `resolveResourceTypeId` créait un `ResourceType` pour tout `typeId` sans vérifier le superType DofusDB. Désormais : vérification via `DofusDbItemTypesCatalogService` et `DofusDbItemSuperTypeMappingService` — seuls les types dont le superType = 9 (Ressource) sont créés dans resource_types ; les consommables (superType 6, 70) et équipements retournent `null`. Sans catalog injecté : retourne l'existant uniquement (aucune création). Pour régénérer les seeders proprement : `php artisan scrapping:types:extract`.
- **Ressources — relations unidirectionnelles (2026-03-19)** : Allègement des requêtes en ne chargeant que les relations « ressources qui composent » (ingrédients). **(1) DofusDB** : `$populate=false` sur fetchOne items pour éviter `recipesThatUse`. **(2) Krosmoz** : `ResourceController` (show/edit) et index ne chargent plus `consumables`, `creatures`, `items` ; uniquement `recipeIngredients`. `ResourceTableController` : retrait de `withCount` pour items, consumables, creatures. `ResourceResource` : exposition uniquement de `recipeIngredients` (relations inverses retirées de l'exposition). **(3) UI** : sections « Objets utilisant cette ressource », « Consommables utilisant », « Créatures » retirées de Show et Edit ; recette (ingrédients) conservée. Routes `updateItems`, `updateConsumables`, `updateCreatures` conservées pour compatibilité API.
- **Admin caractéristiques — Panel Conversion** : select « Fonction de conversion » dans le panneau Conversion (général et par entité), alimenté par `ConversionFunctionRegistry::options()` (création et fiche caractéristique). Option « Aucune » + liste des fonctions enregistrées côté serveur.
- **Système d’effets unifié — Phase 4 (UI admin)** : CRUD admin Sous-effets et Effets (menu Admin : Sous-effets, Effets) ; bouton « Ajouter un degré » sur un effect ; section « Effets » sur les fiches Sort, Item et Consumable (liste des usages, ajout/édition/suppression, aperçu « effet pour niveau X »). Page d’édition Consommable complétée (formulaire + Effets). Voir [PLAN_MISE_EN_OEUVRE_EFFECTS.md](../50-Fonctionnalités/Spell-Effects/PLAN_MISE_EN_OEUVRE_EFFECTS.md).
- **Harmonisation vues Minimal (hover extended)** : [smoke-test-minimal-views-2026-03-04.md](./smoke-test-minimal-views-2026-03-04.md) — Smoke test visuel frontend sur les vues Minimal des entités item, spell, monster, resource. Validation de l'ordre des champs (métier en haut, techniques en bas) après harmonisation. 15 composants analysés, 0 anomalie détectée. Résumé : [smoke-test-minimal-views-summary.md](./smoke-test-minimal-views-summary.md).
- **Export RGPD « Indisponible »** : Correction du problème où l'export restait en « pending » sans date d'expiration tant qu'aucun worker de queue ne traitait le job. Ajout de l'option `PRIVACY_EXPORT_SYNC=true` pour exécuter l'export immédiatement (sans worker), config `config/privacy.php`. Doc mise à jour ([Privacy-RGPD/README.md](../50-Fonctionnalités/Privacy-RGPD/README.md)).
- **Suppression de compte RGPD — délai de rétractation et admin** : (1) Délai de rétractation (7 jours par défaut, configurable via `PRIVACY_ERASURE_WITHDRAWAL_DAYS`) : la demande n'exécute plus le job immédiatement ; la commande `privacy:process-deletion-requests` (planifiée à 02:00) traite les demandes dont le délai est passé. (2) Bannière « Récupérer mon compte » pour les utilisateurs connectés avec une demande en cours. (3) Message explicite après déconnexion et à la tentative de connexion d'un compte supprimé. (4) Admin : boutons Restaurer et Supprimer définitivement (liste + fiche utilisateur) pour les super_admins.
- **Éléments multiples (Spell & Capability)** : Constante numérique 0-29 (Neutre, Terre, Feu, Air, Eau et combinaisons), migration Capability (element string → integer), `ElementConstants` partagé, composant `ElementDisplay` avec dégradés SCSS et icônes (`storage/app/public/images/icons/caracteristics/`). Voir [PLAN_ELEMENTS_MULTIPLES.md](../50-Fonctionnalités/Elements/PLAN_ELEMENTS_MULTIPLES.md).
- **Relations ressources — sens unique (2026-03-19)** : Les ressources ne chargent plus que les relations « ressources qui composent » (recipeIngredients). Relations inverses supprimées : items, consumables, creatures (objets/consommables/créatures utilisant cette ressource). (1) **DofusDB** : `$populate=false` sur `fetchOne` items pour éviter `recipesThatUse`. (2) **ResourceController** : retrait de consumables, items, creatures du `load` (show/edit/index). (3) **ResourceTableController** : retrait de `withCount` items/consumables/creatures. (4) **Edit/Show** : suppression des sections « Objets utilisant cette ressource », « Consommables utilisant cette ressource », « Créatures liées ». (5) **ResourceResource** : retrait de l'exposition whenLoaded pour items, consumables, creatures. Objectif : requêtes plus légères et rapides.
- **Scrapping — corrections recherche (2026-03-09)** : (1) **Panoplies** : `$populate=items` sur l’endpoint item-sets pour inclure les équipements dans la liste ; formatter `extractItemIds` gère désormais les items sous forme d’IDs directs. (2) **Ressources** : `$populate=false` + suppression serveur de `recipesThatUse` (recettes utilisant la ressource) — ces recettes sont obtenues à l’inverse via équipements/consommables. (3) **Classes** : utilisation de `breedSpellsId` de l’API breeds (toutes les variantes) en priorité, fallback spell-levels si breedSpellsId vide ; variantes via `GET /spell-variants?breedId=X` (44 sorts base+variantes). (4) **Sorts invocations** : extraction du monstre invoqué depuis les effets spell-levels (effectId 181 = Invoquer, diceNum = ID monstre DofusDB).
- **Scrapping — pipeline import UX et fiabilité (2026-03-09)** : (1) **Progression visuelle** : fallback automatique en mode synchrone si création de job > 15 s (ex. `QUEUE_CONNECTION=sync`) ou si le job reste en `queued` > 60 s (worker non lancé) ; message explicite « mode synchrone, peut prendre plusieurs minutes » pendant l'attente. (2) **Imports multiples** : guard au clic (ne pas relancer si import en cours) ; annulation du batch précédent avant d'en lancer un nouveau. (3) **Normalisation** : la réponse du fallback synchrone est convertie au format attendu (`run_id`, `summary`, `results`, `debug`). (4) Doc `SCRAPPING_ASYNC_JOBS.md` : prérequis `queue:work` + `QUEUE_CONNECTION=database`.
- **Scrapping — option respect_auto_update (2026-03-09)** : Nouvelle option d'import `respect_auto_update` (défaut `true`). Quand activée, seules les entités déjà en BDD avec `auto_update=true` sont mises à jour par les données de l'API ; celles avec `auto_update=false` sont conservées telles quelles (non remplacées). Option exposée dans le panneau « Options & historique » du dashboard scrapping et transmise aux endpoints batch/jobs.
- **Scrapping — option skip_existing (2026-03-10)** : Nouvelle option `skip_existing` pour ne pas appeler l'API ni faire de calcul sur les entités déjà en BDD qu'on ne remplacerait pas. Accélère fortement les imports en masse.
- **Scrapping — mode de mise à jour unifié (2026-03-10)** : Un seul sélecteur `update_mode` remplace les 3 options (replace_mode, respect_auto_update, skip_existing) pour éviter les contradictions. 4 choix : ignorer si existant, mettre à jour si (brouillon/raw) ET auto_update, mettre à jour tous les auto_update, forcer les mises à jour.
- **Scrapping — fiabilité jobs (2026-03-10)** : (1) `DB::reconnect()` au début de chaque job pour éviter "MySQL server has gone away" après un job long. (2) Retries (2 tentatives, backoff 30 s) pour erreurs transitoires. (3) SaveProgress tous les 5 entités au lieu de chaque entité. (4) Doc : lancer `php artisan queue:work --timeout=660` (le worker utilise 60 s par défaut, ce qui tuait les jobs vers 1 min).
- **Effets — type de cible (direct / piège / glyphe)** : `target_type` au niveau de l'effet (Effect.target_type). Intégration complète : déduplication par signature (target_type + area inclus), API (EffectResource), admin CRUD (champ select), affichage badge dans EffectUsagesManager. Commande `effects:rebuild-signatures` pour recalculer les signatures existantes. Valeur par défaut : direct.
- **Effets — chips avec icônes éléments (2026-03-10)** : Colonne « Effets » du tableau sorts en chips : icône élément (Eau, Feu, Terre, Air, Neutre), valeur, tooltip avec target_type/zone/duration. API `effect_usages_chips` + `buildEffectUsagesData()`. Duration 0 ou 1 → « Immédiat ».
- **Zones d'impact — constantes et icônes (2026-03-10)** : `App\Support\AreaConstants` (PHP) et `@/Utils/Entity/Areas` (frontend) pour uniformiser les zones (point, line, cross, circle, rect). Icônes dans `icons/areas/`. Affichage zone avec icône : colonne Zone tableau sorts, profil sort, EffectUsagesManager, admin Effets. Spell::AREAS_SHAPE délègue à AreaConstants::SHAPE_ID_MAP.
- **Scrapping — catégorie des sorts (2026-03-10)** : Par défaut, tous les sorts sont importés comme **sorts de monstre** (`Spell::CATEGORY_CREATURE`). Seule exception : les sorts ajoutés lors du scrapping des **classes** (breeds) sont marqués en **sorts de classe** (`Spell::CATEGORY_CLASS`). Le hint `spell_category_hint` (Orchestrator → IntegrationService) prioritaire sur la valeur convertie ; défaut CREATURE si absent.
- **Affichage des effets (2026-03-10)** : (1) **Tableau sorts** : nouvelle colonne « Effets » affichant le résumé des effect_usages (nom effet, degré, sous-effets). API table : `effect_usages_summary` chargé via relation `effectUsages.effect.effectSubEffects.subEffect`. (2) **Vue dédiée sous-effets** : page Admin « Sous-effets » (`admin/sub-effects`) listant le référentiel (slug, type, template, nb effets). Lien menu Admin à côté d’« Effets ».
- **Scrapping — recherche par niveau monstres (2026-03-09)** : Le filtre `levelMin` / `levelMax` ne fonctionnait pas pour les monstres car l’API DofusDB stocke le niveau dans `grades.0.level` et non en champ racine. Correction : option `filters.levelPath` dans la config d’entité (ex. `grades.0.level` pour monster). Items et panoplies utilisent `level` par défaut.
- **Images scrapping — stockage MEDIA_PATH (2026-03)** : Correction du `ModelAwarePathGenerator` : résolution des alias du morphMap (`spell`, `item`, etc.) pour utiliser les dossiers MEDIA_PATH des modèles (`images/entity/spells/`, `images/entity/items/`, etc.). `EnsureDirectoryMediaFilesystem` crée explicitement les dossiers avant écriture (évite les échecs si dossiers absents). Les nouvelles images scrapping sont stockées au bon endroit. Commande `media:fix-storage-paths` pour migrer les médias existants stockés à la racine. Dépannage : `config:clear`, `queue:restart` (voir SPATIE_MEDIA_LIBRARY.md).
- **Scrapping — visibilité progression (2026-03)** : Progression enrichie pour `scrapping:run` : affichage de l'ID courant (`Import spell (id=12345)`), heartbeat toutes les 100 entités, `DB::reconnect()` toutes les 500 pour éviter « MySQL gone away », affichage des relations en cours (`[relations] monster id=...`) pendant le drain des dépendances (invocations, drops, etc.). Un sort avec invocation peut déclencher l'import du monstre invoqué et de ses sorts/drops — la barre reste sur le même sort pendant ce temps ; les lignes `[relations]` indiquent que le traitement continue.
- **project:init / init — récupération des types (2026-03-19)** : La Phase 3 de `project:init` (alias `init`) récupère désormais l’ensemble des types depuis DofusDB : (1) **Types item** — ressources, consommables, équipements via `scrapping:types:seed` (item-types API) ; (2) **Races monstres** — catalogue complet via `scrapping:races:seed` (monster-races API). Nouvelle commande `scrapping:races:seed` pour synchroniser `monster_races` depuis l’API. Option `--skip-types` permet de désactiver toute la phase. Utilisation : `php artisan init` ou `php artisan project:init`.
- **project:init / init — récupération des types (2026-03-19)** : La Phase 3 de `project:init` (alias `init`) intègre désormais la récupération complète des types DofusDB : (1) **Types item** : `scrapping:types:seed` (ressources, consommables, équipements via item-types). (2) **Races monstres** : `scrapping:races:seed` — nouvelle commande récupérant toutes les races depuis l’API `/monster-races` et les synchronisant en base (`monster_races` avec `dofusdb_race_id`). Option `--skip-types` pour ignorer l’ensemble (types item + races). L’alias `init` permet d’exécuter `php artisan init`.
- **project:init — mode ignore par défaut (2026-03)** : Par défaut `--update-mode=ignore` : les entités déjà en base ne sont pas remplacées, ce qui permet de reprendre rapidement après un arrêt. `--skip-existing` activé : pas d'appel API pour les entités qu'on n'écraserait pas (gain de temps majeur). Option `--update-mode` pour personnaliser : `draft_raw_auto_update`, `auto_update`, `force`. Correction : le sort avec dofusdb_id ne force plus le remplacement quand `replace_mode=never`.
- **project:init — récupération des types (2026-03-19)** : La Phase 3 de `project:init` (ou alias `init`) inclut désormais la récupération complète des types depuis DofusDB : (1) **Types item** via `scrapping:types:seed` — ressources, consommables, équipements (item-types) ; (2) **Races monstres** via la nouvelle commande `scrapping:races:seed` — catalogue `/monster-races` synchronisé en base (`monster_races`). Option `--skip-types` pour ignorer l’ensemble. Alias `php artisan init` disponible.
- **Commandes project:init et project:update (2026-03-09)** : (1) **project:init** : initialisation complète du projet (migrations, seeders, types, scrapping ordonné par entité). Vide la queue avant scrapping (sauf `--skip-clear-queue`). Compatible exécution longue (set_time_limit(0), DB::reconnect). Notifie admin/super_admin à la fin (succès, durée, heure). Options : `--fresh`, `--skip-migrate`, `--skip-seeders`, `--skip-scrapping`, `--skip-capabilities`, `--skip-types`, `--noimage`, `--skip-cache`, `--entity=`, `--max-items=`, `--simulate`, `--init-scheduler`, `--skip-clear-queue`, `--skip-notify`. (2) **project:update** : idem (queue, long run, notification). Options : `--entity=`, `--noimage`, `--skip-cache`, `--dry-run`, `--skip-clear-queue`, `--skip-notify`. (3) **Scheduler** : `PROJECT_UPDATE_CRON` (défaut `0 1 1 * *`). (4) **Notification** : type `project_maintenance` (config notifications), canaux par défaut in-app.
- **Pagination côté serveur — tableaux (2026-03-10)** : Nouveau mode `server-side` pour les tableaux d'entités avec gros volumes (10k+ lignes). Au lieu de charger 5000 lignes puis filtrer/trier côté client, l'API reçoit `page`, `limit`, `sort`, `order`, `search`, `filters[key]` et renvoie la page courante + `meta.pagination`. Sorts activés en premier. Voir [TANSTACK_TABLE.md](../30-UI/TANSTACK_TABLE.md).
- **Tableau sorts — colonne Profil et largeur Effets (2026-03-10)** : (1) Colonne **Profil** regroupe PO, PA, zone d'effet (ex‑« Zone »), élément et niveau pour un affichage compact ; les colonnes individuelles PO, PA, zone, élément, niveau sont masquées par défaut. (2) Ordre des colonnes : Nom → Profil → Effets (point central du sort). (3) Profil et Effets ont une largeur étendue (`min-w-[18rem] max-w-3xl`) pour une meilleure lisibilité. (4) Support de `table.order` dans les descriptors pour contrôler l'ordre des colonnes. (5) Libellé « Zone d'effet » pour la zone d'impact (plus compréhensible).
- **Tableau sorts — affichage du degré dans les effets (2026-03-10)** : La colonne Effets affiche désormais le degré (D1, D2, D3…) en préfixe de chaque chip et dans le tooltip (« Degré X »), permettant de distinguer les grades des sorts multi-niveaux. `buildEffectUsagesData()` enrichit les chips avec `degree` et le préfixe texte.
- **SpellEffectChips — filtre par degré (2026-03-10)** : Composant dédié à l'affichage des effets dans le tableau sorts. Par défaut, seul le degré 1 (ou les effets sans degré) est affiché. Un bouton « + D2, D3 » permet d'afficher les degrés supérieurs ; un clic sur « − D2, D3 » les masque à nouveau. Limite le bruit visuel pour les sorts multi-grade.
- **Vue Line / Minimal / Colonne (2026-03-11)** : Trois vues distinctes dans les tableaux : (1) **Ligne** — liste dense verticale (ResourceLineRow pour ressources, *ViewMinimal en colonne pour les autres) ; (2) **Minimal** — grille flex-wrap de cartes (~280px, gap 3) ; (3) **Colonne** — tableau classique. Boutons « Ligne » | « Minimal » | « Colonne » à côté de la densité.
- **Affichage des caractéristiques selon la vue (2026-03-11)** : Adaptation de l'affichage des effets/caractéristiques selon le mode de vue : **Line** et **Large** — nom complet ; **Minimal** — icône seule ; **Compact** — abréviation (short_name). Dans tous les cas, le tooltip affiche le nom complet. Prop `labelMode` (`full` | `short` | `icon-only`) sur CharacteristicEffectsGrid, CharacteristicChip, CharacteristicInlineGroup ; dérivé du `context` dans `buildCharacteristicEffectCell`.
- **Affichage caractéristiques selon la vue (2026-03-11)** : Adaptation de l'affichage des effets/caractéristiques selon le mode de vue. **Line** et **Large** : nom complet + valeur. **Minimal** : icône seule (tooltip avec nom complet). **Compact** : abrégé (short_name) + valeur. Dans tous les cas, le tooltip affiche le nom complet. Props `labelMode` (`full` | `short` | `icon-only`) sur CharacteristicEffectsGrid, CharacteristicChip, CharacteristicInlineGroup ; dérivé automatiquement du `context` (extended/compact/minimal) dans `buildCharacteristicEffectCell`.
- **Effets / caractéristiques — labelMode selon la vue (2026-03-11)** : Affichage des caractéristiques adapté au mode de vue : **Line** et **Large** → nom complet ; **Compact** → abrégé (short_name) ; **Minimal** → icône seule. Le tooltip affiche toujours le nom complet. `buildCharacteristicEffectCell` enrichit chaque item (`name`, `shortLabel`, `tooltip`) et calcule `labelMode` depuis `options.context` (extended → full, compact → short, minimal → icon-only). `CharacteristicEffectsGrid`, `CharacteristicChip` et `CharacteristicInlineGroup` reçoivent la prop `labelMode`. CellRenderer transmet `params.chipsLayout.labelMode` aux chips.
- **Affichage des effets selon la vue (2026-03-11)** : Les chips de caractéristiques (effets items, ressources, consommables) s’adaptent au mode de vue. (1) **Vue Line et Large** : nom complet (ex. « Vitalité : 8 »). (2) **Vue Compact** : abrégé (ex. « Vita : 8 »). (3) **Vue Minimal** : icône seule (tooltip avec nom complet). Dans tous les cas, le **tooltip** affiche toujours le nom complet. Prop `labelMode` (`full` | `short` | `icon-only`) dérivée du `context` passé à `toCell` (extended → full, compact → short, minimal → icon-only).
- **Affichage caractéristiques selon la vue (2026-03-11)** : Adaptation de l'affichage des effets/caractéristiques (vitalité, agilité, etc.) selon le mode de vue : **Line** et **Large** — nom complet (icône + nom + valeur) ; **Minimal** — icône seule ; **Compact** — abrégé (icône + short_name + valeur). Dans tous les cas, le **tooltip affiche toujours le nom complet**. Prop `labelMode` (`full` | `short` | `icon-only`) sur CharacteristicEffectsGrid, CharacteristicChip et CharacteristicInlineGroup ; dérivé automatiquement du contexte (`extended` → full, `compact` → short, `minimal` → icon-only) via `buildCharacteristicEffectCell`.
- **Affichage des ingrédients (ressources de recette) — Items, Consumables, Resources (2026-03-11)** : Les équipements (items), consommables et ressources peuvent avoir des ressources comme ingrédients. Affichage via `ResourceIngredientsList` (icône + nom × quantité, tooltip minimal). **Vue Minimal** : ingrédients masqués par défaut, affichés dans l’overlay au survol (slot expanded). **Autres vues** (Line, Large, Compact) : liste affichée sous les effets. Backend : table controllers chargent `resources` (Item, Consumable) et `recipeIngredients` (Resource) en format entities.
- **Vues Minimal — expansion au survol (2026-03-11)** : Composant de base `EntityMinimalCard` pour les cartes Minimal (Item, Resource, Consumable). Mode `hover` : contenu compact par défaut (image, nom, type, rareté, prix, effets) ; au survol, overlay étendue avec description et détails complets. L'overlay passe au-dessus du contenu (z-index 40, sous les tooltips) sans modifier le flux du DOM — les cartes gardent leur place initiale. TanStackTable passe `display-mode="hover"` en mode Minimal pour activer ce comportement.
- **Service overlay unifié + Entity Overlay Card (2026-05-05)** : Refactor complet des tooltips/popovers vers un service unique (`useOverlay`, `OverlayTrigger`, host global), avec mode **click-first** uniforme sur les vues texte d'entités, lazy loading, cache TTL + dedup, accessibilite clavier/ARIA, fermeture controlee (focus/souris/Escape), gestion de pile (z-index, max ouverts, eviction) et suppression des anciens composants non utilises (`EntityMinimalTooltip`, `useEntityHoverCard`). Voir [ENTITY_HOVER_CARD.md](../30-UI/ENTITY_HOVER_CARD.md).
- **Authentification OAuth — GitHub, Discord et Steam (2026-03-13)** : Connexion et inscription via GitHub, Discord et Steam en complément du formulaire classique. Liaison/déliaison des providers depuis Paramètres → Connexions. Conversion d'un compte OAuth-only en compte classique (ajout mot de passe). Table `oauth_accounts`, colonne `users.password` nullable. Voir [OAUTH_AUTHENTICATION.md](../00-Project/OAUTH_AUTHENTICATION.md).
- **OAuth — confirmation liaison et mot de passe (2026-03-13)** : (1) **Confirmation avant liaison** : quand l'email OAuth correspond à un compte existant, page de confirmation « Voulez-vous lier ce compte ? » au lieu de liaison automatique. (2) **Définir un mot de passe** : sur Mon compte, les utilisateurs OAuth-only voient « Définir un mot de passe » sans champ « mot de passe actuel ». Lien « Gérer les connexions » vers Paramètres#connections.
- **Système de retour utilisateur (feedback) (2026-03-13)** : Bouton FAB en bas à droite permettant de signaler un bug, une erreur, une suggestion ou autre. Modal avec URL courante, pseudo (pré-rempli si connecté), type, message et pièce jointe optionnelle. Envoi par email aux admins. Route `POST /feedback` throttlée. Voir [FEEDBACK_SYSTEM.md](../00-Project/FEEDBACK_SYSTEM.md).
- **Mot de passe oublié (2026-03-13)** : Flux de réinitialisation uniquement pour les comptes ayant un mot de passe (pas OAuth-only). Les comptes Discord/Steam/GitHub uniquement reçoivent un message les invitant à utiliser leur provider habituel. Pages ForgotPassword et ResetPassword harmonisées (lien retour connexion, styles). Voir [OAUTH_AUTHENTICATION.md](../00-Project/OAUTH_AUTHENTICATION.md).
- **Modificateurs et jets de sauvegarde des créatures (2026-03-14)** : Les créatures disposent désormais des modificateurs (Force, Intel, Agi, Chance, Sagesse, Vitalité) et des jets de sauvegarde complets selon les règles 2.2.2 : **1d20 + mod. carac. + bonus maîtrise (si maîtrisé) + bonus équipement** (max 7 + 6 + 3 = 16). Migration : 12 colonnes sur `creatures` (`save_*_bonus` 0–3, `save_*_mastery` 0/1). Formules `characteristic_creature` mises à jour. Affichage dans la carte caractéristiques (section « Modificateurs et sauvegardes ») avec détail mod + maîtrise + équip.
- **Service universel d'affichage des caractéristiques (2026-03)** : Centralisation de la logique d'affichage (icône, couleur, container) dans `useCharacteristicDisplay` et `Utils/color/Color.js`. Suppression des duplications : `hexToRgba` (3×), `getCharacteristicColorStyle` (4×), `getCharacteristicContainerStyle` (3×). Composants migrés : CharacteristicFormula, CharacteristicBoolean, CharacteristicBadges, CharacteristicChip, CharacteristicEffectsGrid, PropertyDisplay. Voir [AUDIT_SERVICE_AFFICHAGE_CARACTERISTIQUES.md](../50-Fonctionnalités/Characteristics-DB/AUDIT_SERVICE_AFFICHAGE_CARACTERISTIQUES.md).
- **Service caractéristiques global (2026-03-14)** : Chargement des métadonnées des caractéristiques (icon, color, name, description, value_available) une seule fois au démarrage via Inertia share. Les API Table ne renvoient plus `meta.characteristics`. Le frontend utilise `useCharacteristicsStore` et `getEntityCharacteristicsByDbColumn` pour résoudre les définitions. Endpoint `GET /api/characteristics` disponible pour fetch alternatif. Voir [AUDIT_SERVICE_AFFICHAGE_CARACTERISTIQUES.md](../50-Fonctionnalités/Characteristics-DB/AUDIT_SERVICE_AFFICHAGE_CARACTERISTIQUES.md).

- **Scrapping — types de ressource par superType (2026-03)** : Correction du formatter `resolveResourceTypeId` : il ne crée désormais des `ResourceType` que pour les `typeId` dont le `superTypeId` DofusDB = 9 (Ressource). Les consommables (superTypes 6, 70) et équipements (autres) retournent `null` au lieu de polluer `resource_types`. Injecté : `DofusDbItemTypesCatalogService` et `DofusDbItemSuperTypeMappingService` dans `FormatterApplicator`. Sans ces services (ex. tests), seul un type existant est retourné, jamais créé. Pour régénérer les seeders à partir de l'API : `php artisan scrapping:types:extract`.
- **Scrapping — types de ressources (superType) (2026-03)** : Correction du formatter `resolveResourceTypeId` qui créait des `ResourceType` pour tous les `typeId` (consommables, équipements). Désormais, seuls les types dont le superType DofusDB = 9 (Ressource) sont créés/retournés. Les consommables (superType 6, 70) et équipements retournent `null`. Injection de `DofusDbItemTypesCatalogService` et `DofusDbItemSuperTypeMappingService` dans `FormatterApplicator`. Sans catalog en contexte (ex. tests), seul un type existant est retourné (jamais de création). Voir [ITEM_TYPES_REFERENCE.md](../50-Fonctionnalités/Scrapping/Architecture/ITEM_TYPES_REFERENCE.md). Pour régénérer les seeders propres : `php artisan scrapping:types:extract`.
- **project:init — récupération des types (2026-03)** : La Phase 3 inclut désormais la récupération de tous les types DofusDB : (1) **Types item** (ressources, consommables, équipements) via `scrapping:types:seed`. (2) **Races monstres** via la nouvelle commande `scrapping:races:seed` qui récupère le catalogue `/monster-races` depuis l'API DofusDB et synchronise la table `monster_races` (création ou mise à jour du nom pour les races existantes, sans réinitialiser state/read_level/write_level). Option `--skip-types` pour désactiver l'ensemble. Alias `init` disponible : `php artisan init` équivaut à `php artisan project:init`.
- **Uniformisation UI (glass, carré, sombre) (2026-03)** : Harmonisation du design system pour un rendu glass, carré et sombre. (1) **Fallbacks radius** : `GlassMenuPanel` et `Admin/characteristics` — fallback `var(--radius-box, 0.125rem)` aligné avec le thème. (2) **Documentation** : `DESIGN_GUIDE.md` mis à jour (radius 0.125rem, note style glass/carré/sombre). (3) **Radius** : remplacement de `rounded-lg` et `rounded-xl` par `rounded-box` sur les containers (cartes, panels, formulaires, modales) — SidebarNav, TanStackTable, LineRows, Settings, Login, Admin, EffectUsagesManager, formulaires utilisateur, ViewMinimal, etc. Conservation de `rounded-full` pour avatars et indicateurs circulaires. (4) **Mappings** : `data-displayMap.js` — ajout `box: "rounded-box"` ; `feedbackMap.js` — skeletons 2xl/3xl/4xl/image passés en `rounded-box`.
